# ─── Tempo Server ────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# This file runs on Railway — never commit the real .env to git.

# ─── Supabase ────────────────────────────────────────────────────────────────
# supabase.com → your project → Settings → API
SUPABASE_URL=
# Service role key — has full DB access, server-side only, never in the app
SUPABASE_SERVICE_ROLE_KEY=

# ─── Twelve Labs ─────────────────────────────────────────────────────────────
# twelvelabs.io → Dashboard → API Keys
TWELVE_LABS_API_KEY=

# ─── Google Cloud ────────────────────────────────────────────────────────────
# Google Cloud Console → APIs & Services → Credentials → API Keys
# Make sure YouTube Data API v3 is enabled on this project
YOUTUBE_DATA_API_KEY=

# ─── Cobalt (fallback downloader) ────────────────────────────────────────────
# The internal Railway URL of your deployed Cobalt service.
# e.g. https://tempo-cobalt.railway.internal
COBALT_URL=

# ─── Server config ────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development

# ─── Dev bypass (local development only) ─────────────────────────────────────
# Skips auth token verification so you can hit the server without signing in
DEV_BYPASS_AUTH=false
# UUID of a user in your Supabase auth.users table — used when bypass is on
DEV_USER_ID=
